import mount from "moon/src/view/mount";
import components from "moon/src/view/components";
import normalizeChildren from "moon/src/view/normalizeChildren";

export default {
	components,
	mount,
	normalizeChildren
};
